import { AppWindow } from './app-window';

describe('AppWindow', () => {
  it('should create an instance', () => {
    const directive = new AppWindow();
    expect(directive).toBeTruthy();
  });
});
